@model List<RaceLibrary.Participant>

@foreach (var part in Model)
{
    <br />
    @Html.Label(part.Name)
    <br />
    string progress = "";
    for (int i = 0; i < part.Vote; ++i)
    {
        progress += "|";
    }
    @Html.Label(progress)

}

